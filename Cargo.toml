[workspace]
members = [
    "school-core",
    "api-server",
    "local-bridge",
    "hash-gen"
]
default-members = ["api-server", "school-core", "local-bridge"]

resolver = "2"

[profile.dev]
debug = 1

[profile.release]
opt-level = 3
lto = false
codegen-units = 16
panic = "abort"
strip = true

