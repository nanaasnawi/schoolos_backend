[workspace]
members = [
    "school-core",
    "api-server",
    "local-bridge",
    "hash-gen"
]
default-members = ["school-core", "local-bridge"]

resolver = "2"

[profile.dev]
debug = 1

[profile.release]
opt-level = 3
lto = "thin"
codegen-units = 1
panic = "abort"
strip = true

